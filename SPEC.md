# 仕様書 — 統計検定2級 学習アプリ

| 項目 | 内容 |
|------|------|
| アプリ名 | 統計検定2級 学習アプリ |
| バージョン | 1.3.0 |
| 公開 URL | https://korosk.github.io/toukei-2kyuu-app/ |
| リポジトリ | https://github.com/koroSK/toukei-2kyuu-app |
| 対象ユーザー | 統計検定2級の取得を目指す初学者〜中級者（数学苦手者含む） |
| 対応デバイス | PC・スマートフォン・タブレット（レスポンシブ対応） |

---

## 1. 画面構成

```
ホーム画面（ダッシュボード）
├── 学習モード
│   ├── 単元一覧（11単元・進捗バー付き）
│   └── 単元詳細（フォーカスモード対応）
│       └── 単元テスト → 結果・解説 + 紙吹雪エフェクト
├── 試験モード（35問・90分）→ 結果・解説 + 紙吹雪エフェクト
└── 進捗管理（単元別正解率・受験履歴）
```

---

## 2. デザインシステム

### 2-1. カラーパレット（Immersive Dark Learning System）

| 変数名 | 値 | 用途 |
|---|---|---|
| `--bg-base` | `#121212` | ページ背景（Rich Black） |
| `--bg-surface` | `#1a1a2e` | カード・パネル背景 |
| `--bg-card` | `#1e1e32` | カード背景 |
| `--primary` | `#0f9fff` | Electric Blue（信頼・知性） |
| `--accent` | `#00e676` | Neon Green（正解・完了） |
| `--coral` | `#ff6b8a` | Soft Coral（間違い・警告） |
| `--amber` | `#ffab40` | Amber（フラグ・注意） |
| `--text` | `#e0e0e0` | オフホワイト（目の疲労軽減） |
| `--text-muted` | `#6b6b80` | 補助テキスト |

### 2-2. エフェクト

| エフェクト | 実装 |
|---|---|
| ニューモーフィズム | `box-shadow: 6px 6px 14px #0a0a14, -6px -6px 14px #22223e` |
| グラスモーフィズム | `backdrop-filter: blur(20px)` + 半透明背景 |
| Neon グロー | `0 0 20px rgba(15,159,255,0.2)` |
| フォーカスモード | `.focus-mode .app-header { opacity: 0; transform: translateY(-100%); }` |
| 紙吹雪 | JS で DOM に `.confetti-particle` を生成、CSS `animation: confetti-fall` |

### 2-3. タイポグラフィ

- **フォント**：Noto Sans JP（Google Fonts）
- **行間**：1.7（body）
- **見出し**：font-weight 700〜900

---

## 3. 機能仕様

### 3-1. 共通

| 機能 | 仕様 |
|------|------|
| 電卓 | 全画面のヘッダーから 🔢 ボタンで開閉。四則演算・パーセント・符号反転。ニューモーフィックダークデザイン。 |
| 数式描画 | KaTeX（CDN）。`\( \)` でインライン、`\[ \]` でブロック数式。 |
| データ保存 | localStorage。学習進捗・受験履歴をブラウザに永続化。サーバー不要。 |
| レスポンシブ | ブレークポイント 600px・700px。スマホでは縦並びレイアウト。 |

---

### 3-2. 学習モード

#### ダッシュボード（ホーム画面）

- ヒーローエリア：グラスモーフィズムパネルで学習状況を提示
- 統計バー：総問題数・学習済み・受験回数・直近スコアをグラデーション数値で表示
- モードカード：ニューモーフィズム + ホバー時 Neon グロー

#### 単元一覧画面

- 全 11 単元をグリッドカード形式で表示
- 進捗バー：Neon Green グラデーション + Spring アニメーション
- 正解率をアクセントカラーで表示

#### 単元詳細画面

- 「例題先・式は後から」方式の解説
- `.example-box`：Electric Blue の左ボーダー（具体例）
- `.key-point`：Amber の左ボーダー（重要事項）
- **フォーカスモード**：右下の ⛶ ボタンでヘッダーを非表示にして集中学習
  - `.focus-mode` クラスをbodyに付与
  - ヘッダーが opacity: 0 + translateY(-100%) でスムーズに消える

#### 単元テスト

- 全問題を Fisher-Yates シャッフル + 選択肢もランダム化
- 正解・不正解を Neon Green / Soft Coral で色分け
- 解答結果を localStorage に保存
- **正解率 70% 以上で紙吹雪エフェクト**（`launchConfetti()` 関数）

---

### 3-3. 試験モード

#### 出題ロジック

| 項目 | 仕様 |
|------|------|
| 問題数 | 35 問 |
| 制限時間 | 90 分 |
| 出題 | 全 11 単元から下記配分で抽出後シャッフル |
| 選択肢順 | ランダム（毎回変わる） |

**単元別出題配分（合計 35 問）**

| 単元 | 出題数 |
|------|:---:|
| 1. データの種類と記述統計 | 4 |
| 2. 確率の基本・ベイズの定理 | 3 |
| 3. 確率分布（離散型） | 3 |
| 4. 確率分布（連続型） | 4 |
| 5. 標本分布・中心極限定理 | 3 |
| 6. 統計的推定 | 3 |
| 7. 統計的仮説検定 | 5 |
| 8. 分散分析（ANOVA） | 3 |
| 9. 回帰分析・相関分析 | 3 |
| 10. ノンパラメトリック検定 | 2 |
| 11. 時系列分析の基礎 | 2 |

#### 試験画面

| 機能 | 仕様 |
|------|------|
| タイマー | 90:00 からカウントダウン。残り 5 分で Soft Coral + 点滅。0:00 で自動採点。 |
| タイマーデザイン | モノスペースフォント + ニューモーフィックピル形状 |
| 問題ナビ | 左サイドバー。未回答（暗色）・回答済（Neon Green）・フラグ（Amber）で色分け。 |
| 選択肢 | ホバーで Electric Blue ボーダー + translateX(3px) + Neon グロー |
| フラグ機能 | Amber ハイライトでマーク |
| 途中中断 | 確認ダイアログあり |

#### 結果・解説画面

| 項目 | 仕様 |
|------|------|
| スコア | 正解数 / 35 問・正解率（%） |
| カラーバー | 合格（Neon Green グロー）/ 不合格（Soft Coral）でトップ装飾 |
| 合否判定 | 正解率 70% 以上で「合格ライン到達」 |
| 紙吹雪 | 70% 以上 かつ 時間切れでない場合に発火 |
| 単元別正解率 | Electric Blue グラデーションの棒グラフ |
| 全問解説 | 各問の選択肢・正解・解説を展開表示 |

---

### 3-4. マイクロインタラクション

#### 紙吹雪エフェクト（`launchConfetti()`）

```javascript
// 発火条件：単元テスト・試験モードで正解率70%以上
// 実装：28個の .confetti-particle を DOM に生成
// 色：Electric Blue・Neon Green・Amber・Soft Coral など6色
// アニメーション：CSS @keyframes confetti-fall（1.2s forwards）
```

#### プログレスバーアニメーション

```css
/* width: 0 → 実際の % への Spring アニメーション */
transition: width .6s cubic-bezier(.34,1.56,.64,1);
```

#### フォーカスモード

```javascript
// 発火：単元詳細画面の右下 ⛶ ボタン
// 実装：document.body.classList.toggle('focus-mode')
// CSS：.focus-mode .app-header { opacity: 0; transform: translateY(-100%); }
```

---

## 4. 学習コンテンツ仕様

### 4-1. 解説方針

| 方針 | 内容 |
|---|---|
| 例題先 | 数式を見せる前に具体的な数値で手順を確認 |
| ステップ付き計算 | 計算手順を①②③と番号付きで分解 |
| 日常の例え | 年収・サイコロ・気温・医療検査など身近な例を使用 |
| 「なぜ？」の解説 | n-1 で割る理由、余事象を使う理由など背景を説明 |
| 視覚的分類 | `.key-point`（重要事項）と `.example-box`（具体例）で明確に区別 |

### 4-2. 問題バンク

| 項目 | 内容 |
|---|---|
| 総問題数 | 115 問 |
| 形式 | 4択選択式 |
| フィールド | `id`・`unit`・`unitName`・`question`・`options[4]`・`answer`（正解インデックス）・`explanation` |

---

## 5. ランダム性の仕様

| 対象 | ランダム化の方法 |
|------|------|
| 試験の出題問題 | 単元ごとに配分数をシャッフル抽出 → 全体を再シャッフル |
| 試験の問題順 | Fisher-Yates アルゴリズム |
| 選択肢の並び順 | `[0,1,2,3]` をシャッフルして並び替え。正解インデックスも追随。 |
| 単元テストの問題順 | Fisher-Yates アルゴリズム |

---

## 6. データ構造（localStorage）

### `toukei2_progress`
```json
{
  "unitAnswers": {
    "1": 2,
    "3": 0
  }
}
```

### `toukei2_exam_history`
```json
[
  {
    "date": "2026/5/11",
    "score": 24,
    "total": 35,
    "timeLeft": 1820,
    "timeUp": false
  }
]
```

---

## 7. ファイル構成

```
toukei-2kyuu-app/
├── index.html              # エントリーポイント（Noto Sans JP・KaTeX CDN 読み込み）
├── README.md               # プロジェクト概要
├── SPEC.md                 # 本仕様書
├── css/
│   └── style.css           # デザインシステム（ダーク・ニューモーフィズム・グラスモーフィズム）
└── js/
    ├── app.js              # 状態管理・画面描画・フォーカスモード・紙吹雪
    ├── questions.js        # 問題バンク（115 問）
    └── study-content.js    # 学習コンテンツ（数学苦手者向け解説）
```

---

## 8. 外部依存

| ライブラリ | バージョン | 用途 | 読み込み |
|---|---|---|---|
| KaTeX | 0.16.9 | 数式レンダリング | CDN（jsDelivr） |
| Noto Sans JP | — | 日本語フォント | Google Fonts |

---

## 9. 対応ブラウザ

| ブラウザ | 対応状況 | 備考 |
|---|---|---|
| Chrome 最新版 | ✅ | 推奨 |
| Safari 最新版 | ✅ | `-webkit-backdrop-filter` 対応 |
| Firefox 最新版 | ✅ | |
| Edge 最新版 | ✅ | |
| Internet Explorer | ❌ | CSS変数・backdrop-filter 非対応 |

---

## 10. 更新履歴

| バージョン | 日付 | 内容 |
|---|---|---|
| 1.0.0 | 2026-05-08 | 初回リリース（学習・試験・進捗の3モード） |
| 1.1.0 | 2026-05-08 | 学習コンテンツ改訂・電卓グローバル化・選択肢ランダム化・単元バランス出題 |
| 1.2.0 | 2026-05-11 | デザイン全面刷新（Immersive Dark Learning System）・フォーカスモード・紙吹雪エフェクト追加 |
| 1.3.0 | 2026-05-11 | 学習コンテンツを数学苦手者向けに全面改訂（例題先・ステップ付き計算・日常の例え） |
