# 仕様書 — 統計検定2級 学習アプリ

| 項目 | 内容 |
|------|------|
| アプリ名 | 統計検定2級 学習アプリ |
| バージョン | 1.1.0 |
| 公開 URL | https://korosk.github.io/toukei-2kyuu-app/ |
| リポジトリ | https://github.com/koroSK/toukei-2kyuu-app |
| 対象ユーザー | 統計検定2級の取得を目指す初学者〜中級者 |
| 対応デバイス | PC・スマートフォン・タブレット（レスポンシブ対応） |

---

## 1. 画面構成

```
ホーム画面
├── 学習モード
│   ├── 単元一覧
│   └── 単元詳細
│       └── 単元テスト → 結果・解説
├── 試験モード → 結果・解説
└── 進捗管理
```

---

## 2. 機能仕様

### 2-1. 共通

| 機能 | 仕様 |
|------|------|
| 電卓 | 全画面のヘッダーから 🔢 ボタンで開閉。四則演算・パーセント・符号反転に対応。画面右下にフローティング表示。 |
| 数式描画 | KaTeX（CDN）を使用。`\( \)` でインライン、`\[ \]` でブロック数式を描画。 |
| データ保存 | localStorage を使用。学習進捗・受験履歴をブラウザに永続化。サーバー不要。 |
| レスポンシブ | ブレークポイント 600px・680px。スマホでは縦並びレイアウトに切り替わる。 |

---

### 2-2. 学習モード

#### 単元一覧画面
- 全 11 単元をカード形式で表示
- 各カードに学習進捗バー（学習済み問題数 / 全問数）と正解率を表示
- カードをタップ → 単元詳細へ遷移

#### 単元詳細画面
- 各単元のテキスト解説を表示
- セクション構成：導入 → 概念説明（具体例付き） → 重要公式 → ポイントまとめ
- 具体例は青いボックス（`.example-box`）、重要事項は黄色いボックス（`.key-point`）で区別
- 画面下部の「テストを受ける」ボタンで単元テストへ遷移

#### 単元テスト
- 対象単元の全問題を出題
- 出題順：ランダム（Fisher-Yates シャッフル）
- 選択肢順：ランダム（毎回変わる）
- 「採点する」ボタンで全問解説を表示
- 正解・不正解を色分け表示（緑 / 赤）
- 解答結果を localStorage に保存（進捗管理に反映）

---

### 2-3. 試験モード（CBT 形式）

#### 出題ロジック

| 項目 | 仕様 |
|------|------|
| 問題数 | 35 問 |
| 制限時間 | 90 分 |
| 出題方式 | 全 11 単元から下記配分で抽出 → 全体をシャッフル |
| 選択肢順 | ランダム（毎回変わる） |

**単元別出題配分（合計 35 問）**

| 単元 | 出題数 |
|------|:---:|
| 1. データの種類と記述統計 | 4 |
| 2. 確率の基本・ベイズの定理 | 3 |
| 3. 確率分布（離散型） | 3 |
| 4. 確率分布（連続型） | 4 |
| 5. 標本分布・中心極限定理 | 3 |
| 6. 統計的推定 | 3 |
| 7. 統計的仮説検定 | 5 |
| 8. 分散分析（ANOVA） | 3 |
| 9. 回帰分析・相関分析 | 3 |
| 10. ノンパラメトリック検定 | 2 |
| 11. 時系列分析の基礎 | 2 |

#### 試験画面の機能

| 機能 | 仕様 |
|------|------|
| タイマー | 90:00 からカウントダウン。残り 5 分で赤色点滅。0:00 で自動採点。 |
| 問題ナビ | 左サイドバーに 1〜35 の番号ボタン。回答済（緑）・フラグ（黄）・未回答（白）で色分け。 |
| フラグ機能 | 問題ごとにフラグを立てて後で見直せる。 |
| 電卓 | ヘッダーから随時呼び出し可能。 |
| 途中中断 | 「← 中断」ボタン押下時に確認ダイアログを表示。 |
| 未回答チェック | 「採点する」押下時に未回答問題数を警告表示。 |

#### 結果・解説画面

| 項目 | 表示内容 |
|------|------|
| スコア | 正解数 / 35 問・正解率（%） |
| 合否判定 | 正解率 70% 以上で「合格ライン到達」 |
| 単元別正解率 | 棒グラフで各単元の成績を可視化 |
| 全問解説 | 各問題の選択肢・正解・解説を展開表示 |

---

### 2-4. 進捗管理

| 表示項目 | 内容 |
|------|------|
| 単元別進捗 | 学習済み問題数・正解率をプログレスバーで表示 |
| 受験履歴 | 日付・スコア・残り時間を降順で一覧表示 |
| リセット | 確認ダイアログ後に localStorage を全削除 |

---

## 3. 問題バンク仕様

| 項目 | 内容 |
|------|------|
| 総問題数 | 115 問 |
| 形式 | 4 択選択式 |
| 難易度 | 統計検定2級の過去問に準拠した計算問題・概念問題 |
| 構成フィールド | `id`・`unit`・`unitName`・`question`・`options[4]`・`answer`（正解インデックス）・`explanation` |

**問題の種類**

| 種類 | 割合（目安） |
|------|------|
| 計算問題（具体的な数値を計算） | 約 45% |
| 概念問題（定義・性質の理解） | 約 35% |
| 応用・文脈問題（場面に適した手法の選択） | 約 20% |

---

## 4. ランダム性の仕様

| 対象 | ランダム化の方法 |
|------|------|
| 試験の出題問題 | 単元ごとに配分数をシャッフル抽出 → 全体を再シャッフル |
| 試験の問題順 | Fisher-Yates アルゴリズムで全体をシャッフル |
| 選択肢の並び順 | `[0,1,2,3]` をシャッフルして並び替え。正解インデックスも追随。 |
| 単元テストの問題順 | Fisher-Yates アルゴリズムでシャッフル |
| 単元テストの選択肢順 | 試験モードと同様にランダム化 |

---

## 5. データ構造（localStorage）

### `toukei2_progress`
```json
{
  "unitAnswers": {
    "1": 2,
    "3": 0
  }
}
```
- キー：問題の `id`（文字列）
- 値：ユーザーが選択した選択肢のインデックス（0〜3）

### `toukei2_exam_history`
```json
[
  {
    "date": "2026/5/8",
    "score": 24,
    "total": 35,
    "timeLeft": 1820,
    "timeUp": false
  }
]
```

---

## 6. ファイル構成

```
toukei-2kyuu-app/
├── index.html              # エントリーポイント（KaTeX CDN 読み込み）
├── README.md               # プロジェクト概要
├── SPEC.md                 # 本仕様書
├── css/
│   └── style.css           # 全スタイル定義（CSS 変数・レスポンシブ対応）
└── js/
    ├── app.js              # 状態管理・画面描画・電卓・ランダム化ロジック
    ├── questions.js        # 問題バンク（QUESTIONS 配列）
    └── study-content.js    # 学習コンテンツ（STUDY_CONTENT 配列）
```

---

## 7. 外部依存

| ライブラリ | バージョン | 用途 | 読み込み方法 |
|------|------|------|------|
| KaTeX | 0.16.9 | 数式レンダリング | CDN（jsDelivr） |

サーバー・ビルドツール・パッケージマネージャーは不使用。`index.html` をブラウザで直接開くだけで動作する。

---

## 8. 対応ブラウザ

| ブラウザ | 対応状況 |
|------|------|
| Chrome（最新版） | ✅ 対応 |
| Safari（最新版） | ✅ 対応 |
| Firefox（最新版） | ✅ 対応 |
| Edge（最新版） | ✅ 対応 |
| Internet Explorer | ❌ 非対応 |

---

## 9. 更新履歴

| バージョン | 日付 | 内容 |
|------|------|------|
| 1.0.0 | 2026-05-08 | 初回リリース（学習・試験・進捗の3モード） |
| 1.1.0 | 2026-05-08 | 学習コンテンツを初学者向けに改訂、電卓のグローバル化、選択肢ランダム化、単元バランス出題を追加 |
